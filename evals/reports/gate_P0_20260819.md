# Gate sign-off — P0 Foundation & Freeze

**Date:** 2026-08-19
**Mechanical report:** `gate_phase0_20260819.md` (37/37 checks green)
**Codebook version:** `v1:718e9f3e` — frozen 2026-08-19, before any scoring (FR-5.6)
**Verdict:** **PASSED for every item not requiring third-party credentials.** Four items are blocked on account access and are listed below.

---

## Checks

| ID | Check | Result |
|---|---|---|
| P0-1 | `schema.sql` applies clean; all arch §4.1 + Appendix A tables present; idempotent | ✅ 25 tables, 1 view |
| P0-2 | Codebook loads; 33 codes; no code missing `boundary_note`, `journey_rank`, `phase`, `outcome`, `solvable_without_money` | ✅ 7·8·14·4 = 33 |
| P0-3 | `journey_rank` is a total order, no ties within a stage; Stage C phase gating holds | ✅ eliminator → confidence → trigger |
| P0-4 | Contradiction matrix present; C9/C11 exclude Confidence-phase codes; legitimate pairs *not* flagged | ✅ |
| P0-5 | Codebook hash frozen; loader **raises** on a mutated file | ✅ `CodebookError: CODEBOOK MUTATED AFTER FREEZE` |
| P0-6 | All four collectors authenticate, ≥1 record smoke fetch | ⛔ **BLOCKED** — Reddit + YouTube credentials |
| P0-7 | One live OpenAI call writes a `runs` row with real token counts and cost | ⛔ **BLOCKED** — OpenAI key; `MODEL_RATES` intentionally left empty until confirmed against live pricing |
| P0-8 | OpenAI hard usage cap set at **$30** | ⛔ **BLOCKED** — Arvind's billing console (EC-OPS-3) |
| P0-9 | Blank app live at a public URL; cold start recorded | ⚠️ **PARTIAL** — runs clean locally (health 200, script executes with no errors); public deploy needs the GitHub remote + Streamlit Cloud link |

Additional checks written beyond the plan's P0 list: app/pipeline dependency separation (AR-5), embeddings + secrets gitignored, no real secrets in the repo, fixture structural validation.

## Notes carried forward

1. **`phase: na` on 10 of 33 codes.** The three-value phase enum (Eliminator/Confidence/Trigger) is defined in `problemstatement.md` §5.2 as a **Stage C** attribute. It maps cleanly onto Stage C (all 14) and Stage D (all 4), and onto the five Stage A trigger codes. It does **not** fit the two Stage-A access-friction codes or any of the eight Stage B retrieval codes — those are structural friction with a Defer outcome, and labelling them `eliminator` would break the Eliminator ⇒ Exit invariant while labelling them `trigger` or `confidence` would be simply wrong. They carry `phase: na`. No required cross-tab in FR-5.5 uses phase outside Stage C, so nothing is lost. **Alternative if you want it:** add a fourth value `friction`. That changes a pre-registered framework, so it is your call, and it is free to make now — nothing has been scored.

2. **`MODEL_RATES` is deliberately empty** in `pipeline/common/runs.py`. Cost figures in `architecture.md` §9 are estimates; the table is filled from live pricing at first use and `runs.cost_usd` then becomes the measurement (X-1).

3. **`data/corpus.db` is committed empty** (25 tables, 0 rows) so the app's read path is exercised from the first deploy rather than first contact with data.
